const CartProduct = () => {
  return (
    <div>
      <div className={styles.cardImageWrapper}>
        <img src={card.image} alt={card.title} />
      </div>
    </div>
  );
};

export { CartProduct };
