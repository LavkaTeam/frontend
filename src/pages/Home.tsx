import { HeaderMenu } from '../components/HeaderMenu';
import { HeroSection } from '../components/HeroSection';

const Home = () => {
  return (
    <div>
      <HeaderMenu />
      <HeroSection />
    </div>
  );
};

export { Home };